import React, { useState }  from 'react';
import './skaiciuokle.css';

const Sumazodziais = (props) => {
    return (
        <div>
            { props.suma } (Čia dar reikės padaryti)
        </div>
    );
};

export default Sumazodziais;